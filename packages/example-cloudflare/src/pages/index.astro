---
import { LanguageUtil } from '@coffic/cosy-ui';

// 不要预渲染，因为这是一个动态页面（动态获取当前语言）
export const prerender = false;

const langCode = LanguageUtil.getCurrentLanguage(Astro);

return Astro.redirect(`/${langCode}`);
---
